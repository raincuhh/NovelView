import React from "react";

const BrandTextLogo = (): React.JSX.Element => {
   return (
      <>
         <header className="mt-0 sm:mt-2 text-fs-xl font-weight-lg md:mt-4 sm:text-fs-2xl sm:font-weight-xl">
            NovelView
         </header>
      </>
   );
};

export default BrandTextLogo;
